# Daily OS Project Update - 2026-04-23

## Tasks Completed
1. Added `rtcw` command to `src/stage2.asm` to write values to RTC/CMOS registers.
2. Updated help string to include `rtcw`.
3. Verified command parsing logic for two-argument hex commands.

## Technical Details
- `rtcw <reg_hex> <val_hex>` uses ports 0x70 (index) and 0x71 (data).
- Useful for setting RTC time or modifying CMOS configuration bytes from the shell.
- Complements the `rtc` dump command added yesterday.

## Next Steps
- Implement RTC interrupt (IRQ 8) handler for periodic tasks.
- Basic FAT12 file system reading (actually parsing the root directory).
