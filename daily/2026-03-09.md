# OS Project Daily Log - 2026-03-09

## Today's Progress
- Implemented `seek` command in `stage2.asm`.
- Updated system version to Day 31.
- Added internal tracking for `current_lba` to facilitate future relative reads/writes.
- Verified build with `make`.

## Details
- `seek <lba_hex>`: Mocks moving the disk head by updating an internal LBA pointer.
- Help menu expanded to include the new command.

## Next Steps
- Implement FAT12 directory parsing.
- Move from mock `ls` to a real one.
