# 2026-03-06

## Today's Changes
- Implemented `edit` command in `stage2.asm`.
- The `edit` command allows writing a string directly to a memory address (e.g., `edit 0500 Hello World`).
- Updated `ver` message and `help` menu.
- Re-tested assembly with `make`.

## Technical Details
- Added `cmd_edit` and `msg_edit_help`.
- Implemented `.do_edit` routine which uses `parse_hex_word` for the target address and then copies the remainder of the input buffer until null-termination.

## Next Steps
- Implement `write` command for persistent sector writing (INT 13h, AH=03h).
- Start work on a very basic file system structure (FAT12 header parsing).
