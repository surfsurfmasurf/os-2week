# Day 8: Basic Command Arguments (echo)

## Tasks Completed
- Implemented `strcmp_prefix` helper to allow checking the start of a command.
- Added `echo <text>` command which parses the input buffer after the command prefix.
- Updated `ver` string to reflect Day 8.
- Verified logic for skipping the command prefix and printing the remainder of the buffer.

## Next Steps
- Implement argument splitting (handling multiple spaces).
- Basic memory map display command.
- Filesystem exploration (FAT12).
