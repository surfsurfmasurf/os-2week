# Daily Log - 2026-05-04

## Progress
- Implemented `peek` command in kernel shell.
- The `peek` command inspects the first 4 bytes of the kernel load address (0x1000).
- Added basic hex printing logic to `kernel.c`.
- Updated command list and help message.

## Tasks Done
- [x] Create `peek` command.
- [x] Implement hex conversion for memory visualization.
- [x] Update UI (status and help text).

## Notes
The `peek` command is a precursor to a more robust memory inspection tool. Currently, it's hardcoded to check the kernel start address to verify memory access in the shell.

## Next Steps
- Implement dynamic address input for `peek`.
- Add `poke` command to modify memory.
- Research RTC ports for the `time` command.
