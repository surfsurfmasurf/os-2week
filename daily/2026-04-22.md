# Daily OS Project Update - 2026-04-22

## Tasks Completed
1. Added `rtc` command to `src/stage2.asm` to dump raw RTC registers (0x00 to 0x09).
2. Updated version to v0.1.36.
3. Updated help string and command processing logic to include `rtc`.

## Technical Details
- RTC registers 0x00-0x09 contain seconds, minutes, hours, day of week, day of month, month, year, etc.
- Used `out 0x70, al` and `in al, 0x71` sequence to poll the CMOS/RTC.
- Printed as hex bytes for debugging raw values (often BCD).

## Next Steps
- Implement RTC alarm status check or interrupt handling.
- Refine command parser to support more arguments for existing commands.
