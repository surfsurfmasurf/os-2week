# Daily Log - 2026-05-05

## Task: Implement 'exit' (hlt) command
Added a simple `exit` command to the kernel shell that executes the `hlt` instruction. This provides a way to gracefully stop the CPU from the command line.

## Changes
- Modified `src/kernel.c`:
    - Updated help string to include `(x) exit`.
    - Added command handler for `x` that prints a message and calls `hlt`.

## Next Steps
- Implement real-time clock (RTC) reading for the `time` command.
- Begin work on Day 12 System Calls if roadmap allows.
