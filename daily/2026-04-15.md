# 2026-04-15
- Added `setmode` command to `stage2.asm`.
- Allows switching VGA video modes using BIOS INT 10h AH=00h.
- Usage: `setmode <hex-mode>` (e.g., `setmode 13` for 320x200 256-color).
- Updated `help` and `vga` command behavior.
