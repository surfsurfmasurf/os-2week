# 2026-04-07

- Further optimized the command table in `stage2.asm`.
- Added `cpuid` as an alias for the `cpu` command.
- Removed remaining duplicated dispatch entries for `mkdir`, `cd`, `ps`, etc.
- Incremented version to v0.1.31.
- Verified build with `make`.
