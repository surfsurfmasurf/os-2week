# Daily Log - 2026-04-05

## Progress
- Updated `stage2.asm` to version 0.1.30.
- Implemented `ior` and `iow` commands for word-sized port access.
  - `ior <port_hex>`: Read word from port.
  - `iow <port_hex> <val_hex>`: Write word to port.
- Added `ior` and `iow` to `help` menu.
- Verified build and assembly.

## Changes
- `src/stage2.asm`: Added `.do_ior` and `.do_iow` handlers, `cmd_ior`, `cmd_iow`, and related messages. Updated version string and help text.

## Next Steps
- Implement basic FAT12 root directory parsing for `ls` command.
- Implement `insb`/`outsb` for string-sized port access.
