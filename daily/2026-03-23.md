# 2026-03-23 - Day 45: Diagnostic Tools & Power Management

## Tasks
- Added `lspci` command as an alias for `pci` to improve Linux-like shell familiarity.
- Implemented `poweroff` command targeting common emulator I/O ports (0x604, 0xB004, 0x4004) to allow graceful exit from QEMU/VirtualBox.
- Updated `ver` and `help` strings to reflect new features.

## Results
- Commands verified via build.
- Code size remains well within Stage 2 limits.

## Next
- Real PCI device enumeration (listing class codes/names).
- Simple FAT12 filesystem implementation (Stage 3 goal).
