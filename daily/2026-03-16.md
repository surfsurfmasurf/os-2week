# OS Development Log: 2026-03-16

## Progress
- Implemented `rm` command (mock) to simulate file deletion.
- Implemented `pwd` command (mock) to display the current working directory.
- Updated `help` menu to include the new commands.
- Bumped version to `v0.1.14`.

## Technical Notes
- Added more Unix-like command parity to the shell (`rm`, `pwd`).
- Updated `src/stage2.asm` with new command parsing logic and message strings.
- Successfully built the OS image with the new stage-2 payload.

## Next Steps
- Begin implementing actual FAT12 directory traversal to move beyond mock commands.
- Plan memory management for loading files from disk.
