# 2026-03-19 OS Development Log

## Today's Progress
- **Focused Tasks**:
  - Implemented mock `su` (switch user) and `sudo` commands in the stage 2 shell.
  - Updated `help` command to list new mock utilities.
  - Bumped version to `v0.1.17`.

## Technical Details
- Added `cmd_su` and `cmd_sudo` string definitions.
- Implemented `.do_su_mock` and `.do_sudo_mock` routines.
- Added corresponding success messages.

## Next Steps
- Implement actual user/privilege levels (requires Protected Mode/Ring 3 transition).
- Research real-mode user management possibilities (minimal).
