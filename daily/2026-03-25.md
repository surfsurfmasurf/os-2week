# Daily OS Project Update - 2026-03-25

## Tasks Completed
- Implemented PCI class name translation in `stage2.asm`.
- The `pci` (and `lspci`) command now translates major PCI class codes (Mass Storage, Network, Display, Bridge) into human-readable strings.
- Refined the `pci` loop to correctly handle register preservation across class name lookup.
- Updated version string to v0.1.23.

## Technical Details
- Added a simple comparison/jump table in `.do_pci` to check the Base Class byte against known values.
- Defined new message strings for PCI class categories.
- Optimized register usage: `bl` now stores the Base Class for lookup before printing the class name string.

## What's Next
- Implement sub-class translation for common devices (e.g., distinguish between Ethernet and Token Ring).
- Begin the "Stage 3" groundwork: FAT12 Boot Record parsing to locate the root directory.
