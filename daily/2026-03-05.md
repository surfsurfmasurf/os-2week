# 2026-03-05 (Day 27)

## Progress
- Refactored BIOS disk routines in `stage2.asm` into a shared `lba_to_chs` procedure.
- Updated `cat` and `read` commands to use the new shared disk routine.
- Fixed a code duplication issue where LBA mapping was manually handled in multiple places.
- Incremented version to `v0.1.4`.
- Verified build stability with `make`.

## Next Steps
- Implement FAT12 directory traversal.
- Add a proper `ls` command that reads from the disk.
- Support reading files larger than 512 bytes.
