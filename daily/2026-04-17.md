# Daily Log - 2026-04-17

## Tasks Accomplished
- Added `peek_w` command to the shell for 16-bit word memory inspection.
- Added `poke_w` command to the shell for 16-bit word memory modification.
- Refactored shell command parsing for better organization.
- Built and verified bootable image `build/os.img`.

## Technical Details
- `peek_w <addr>`: Reads 2 bytes from the specified 16-bit offset.
- `poke_w <addr> <val>`: Writes a 16-bit word to the specified offset.
- Both commands utilize existing `parse_hex_word` and `print_hex_byte` helpers.

## Next Steps
- Implement basic FAT12 filesystem browsing (Stage 2 currently uses raw sector reads).
- Prepare for transition to 32-bit Protected Mode (Day 3/9 overlap).
