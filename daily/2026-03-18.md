# Daily Log - 2026-03-18

## Tasks
- Added mock `cp` (copy) command to stage2 shell.
- Added mock `mv` (move) command to stage2 shell.
- Added mock `history` command to stage2 shell.
- Updated `ls` mock output to include a `backup/` directory.
- Bumped version to v0.1.16.

## Details
Today's focus was on expanding the shell's utility command set to mimic more of the standard Linux/MS-DOS file management experience. While these are still mocks, they provide the UI structure for the upcoming FAT12 implementation.

## Next Steps
- Implement actual string argument parsing for `cp` and `mv`.
- Begin work on a simple history buffer to make `history` functional.
