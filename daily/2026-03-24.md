# Daily OS Project Update - 2026-03-24

## Tasks Completed
- Improved PCI device enumeration in `stage2.asm`.
- Added Class Code and Subclass Code to the `pci` / `lspci` command output.
- Fixed a bug in `pci` command where Device and Vendor IDs were printed incorrectly due to stack/register confusion.
- Updated version string to v0.1.22.

## Technical Details
- Modified `.do_pci` to fetch the Class Code from PCI configuration space offset `0x08`.
- Corrected the Vendor:Device ID printing logic to properly handle 16-bit values from the 32-bit `eax` return.
- PCI output now follows the format: `B:D.F VenID:DevID ClassCode`.

## What's Next
- Implement a lookup table or logic to translate PCI class codes into human-readable names (e.g., `0101` -> `IDE Controller`).
- Continue progress toward Stage 3: Simple FAT12 filesystem implementation.
