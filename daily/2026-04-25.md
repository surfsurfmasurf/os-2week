# 2026-04-25 - Day 63: PIC IRQ Unmasking

Today I implemented the `unmask` command in `stage2.asm` to allow manual enabling of IRQs through the PIC (Programmable Interrupt Controller) masks.

## Changes
- Added `unmask <irq_hex>` command to the shell.
- Implemented logic to calculate the bitmask for Master/Slave PICs based on the IRQ number (0-15).
- Added success and help messages for the `unmask` command.

## Next Steps
- Implement an actual RTC IRQ handler (IRQ 8) to test unmasking.
- Explore mapping higher IRQs to see if they fire correctly in protected mode (though we are currently in real mode/stage 2).
