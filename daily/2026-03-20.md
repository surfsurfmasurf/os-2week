# 2026-03-20 OS Development Log

## Today's Progress
- **Focused Tasks**:
  - Implemented mock `uname` command to display system information.
  - Updated `help` command to list `uname`.
  - Bumped version to `v0.1.18`.

## Technical Details
- Added `cmd_uname` and `msg_uname_mock` to `src/stage2.asm`.
- Wired up `.do_uname_mock` routine in the command processor.
- Verified version string update.

## Next Steps
- Implement basic `argc`/`argv` parsing for commands.
- Research Protected Mode transition (GDT initialization).
