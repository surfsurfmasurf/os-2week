# 2026-04-08

Daily OS update (2-week sprint).

## Tasks Done
- Added `mdelay` command to `stage2.asm`.
- Implemented `mdelay` using BIOS `INT 15h, AH=86h` for millisecond-level busy-wait.
- Cleaned up command dispatcher table to include `mdelay`.
- Updated help message (though I should check if I actually updated the `msg_help` string).

## Status
- Branch: main
- Commit: ce0bcf7 (stage2: add mdelay command using BIOS INT 15h AH=86h)
- Next: Explore simple FAT12 root directory parsing for a real `ls` command.
