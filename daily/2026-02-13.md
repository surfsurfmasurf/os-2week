# Day 9: System Memory Map (INT 15h E820h)

## Tasks Completed
- Implemented `mmap` command to query the system memory map.
- Added `print_hex_32` helper function to display 32-bit hex values.
- Integrated BIOS INT 15h, AX=E820h for robust memory detection.
- Updated `ver` and `help` commands.

## Next Steps
- Implement argument splitting (handling multiple spaces).
- Basic FAT12 filesystem exploration (loading root directory).
- Memory allocator (simple bitmap or linked list).
