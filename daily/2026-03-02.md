# Day 24: Mock Filesystem Interface

Today I implemented a mock `ls` command to prepare for the transition to a real FAT12 or custom filesystem.

## Changes
- Added `ls` command to shell.
- Updated version string to `v0.1.1`.
- Updated help menu to include `ls`.
- Mocked file listing: `boot.bin`, `stage2.bin`, `README.txt`.

## Technical Details
The `ls` command currently prints a static string of filenames. This serves as a placeholder for the directory traversal logic that will be added once disk I/O routines are more robust.

## Next Steps
- Implement basic Disk Read (INT 13h) for stage2 to read its own sectors.
- Explore FAT12 structure.
