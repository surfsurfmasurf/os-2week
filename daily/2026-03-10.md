# OS Project Daily Log - 2026-03-10

## Today's Progress
- Added `whoami` command to `stage2.asm`.
- Updated system version to Day 32.
- Cleaned up help menu and added new command entry.
- Verified build with `make`.

## Details
- `whoami`: Returns "Root User (Admin)".
- Incremented version string to v0.1.8.

## Next Steps
- Implement FAT12 directory parsing.
- Move from mock `ls` to a real one.
