# Daily Log - 2026-03-07

## Progress
- Implemented `write` command in `stage2.asm`.
- The `write` command uses BIOS INT 13h, AH=03h to write one sector from memory buffer `2000:0000` back to the disk at a specified LBA.
- Updated help text and version string to v0.1.6.

## Details
- `lba_to_chs` is reused for both `read` and `write`.
- Verified source/destination buffers match (0x2000:0x0000).

## Next Steps
- Implement basic FAT12 directory entry parsing.
- Move from "mock" `ls` to actual file listing.
